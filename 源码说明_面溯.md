# 面溯 · 源码地图（附给 DeepSeek 的说明）

> 这是「面溯 / miansu」求职复盘网站的前端源码包说明。配合《DeepSeek 交接包_面溯.md》一起阅读：
> **交接包讲「做什么、怎么存、怎么算、长什么样」**，**本文件讲「现有代码长什么样、怎么跑、改哪里」**。
> 若你只要 vibe 出新网站，看交接包即可；若要在现有实现上改/加功能/修问题，本文件是地图。

## 0. 一句话定位
纯前端、零后端、零构建的求职面试复盘工具。录音本地转写 → AI 总结 → 结构化 6 模块复盘 → 改写简历 → 数据留在本机。视觉为玻璃拟态双主题（深蓝紫 / 浅蓝），品牌字用思源宋体。

## 1. 文件清单（每个文件一句话职责）

| 文件 | 职责 |
|---|---|
| `index.html` | 入口 / 求职看板：Hero + 环形进度 + 周历 + 时间轴 + 漏斗 + 统计卡 |
| `records.html` | 面试记录列表：新增 / 编辑表单（公司、岗位、轮次、结果、下一场安排等） |
| `detail.html` | 单条记录详情：**双路径获取逐字稿**（粘贴文本 / 上传录音本地转写）→ 可编辑校对 → AI 总结 |
| `review.html` | 面试复盘：6 模块折叠面板（基础信息 / 问答 / 深度复盘 / 意向信号 / 短板 / 后续行动） |
| `resume.html` | 改写简历：调用 P3 Prompt，输入原简历 + 目标岗，产出改写建议 |
| `profile.html` | 个人档案（关于使用者自身；**遵守红线：不出现真实个人信息**） |
| `settings.html` | 设置：代理地址 / 模式切换（DEMO·LIVE）/ 导出·导入备份 / 主题 |
| `about.html` | 关于项目：面溯介绍（**无个人信息，纯产品说明**） |
| `app.js` | 核心逻辑（见 §3） |
| `styles.css` | 设计系统：`:root` CSS 变量驱动双主题、玻璃卡、按钮、漏斗、时间轴、Toast、动效 |
| `proxy-worker.js` | Cloudflare Worker 代理：解决浏览器直连 DeepSeek 的 CORS，可在服务端持有 Key（详见 §6） |
| `_test_parse.mjs` | 逐字稿解析器的单元测试（**18 个用例**，覆盖冒号/括号/中文方头括号/时间戳/匿名标签/标签独占一行/别名+时间戳无冒号/防误判等；`node _test_parse.mjs [源码路径]` 运行，可传线上下载的 `app.js` 验证线上行为。非运行时依赖） |
| `_check.js` | Node 校验脚本（打包前检查，非运行时依赖，可忽略） |
| `_pack.py` | 打包脚本（生成 zip，非运行时依赖，可忽略） |

> 目录内另有 `v1/`、`v2/` 子目录，为历史版本快照，与根目录当前源码功能一致、仅种子公司名不同；发 DeepSeek 时**只发根目录这 12 个文件即可**，`v1/ v2/` 不必附。

## 2. 架构要点

- **技术栈**：vanilla JS，无框架、无打包、无后端。8 个独立 HTML 页面共享 `styles.css` + `app.js`。
- **导航 / 深链**：页面间通过 `?job=<id>` 传递当前面试记录 ID（如 `detail.html?job=xxx`）。
- **数据存储**：`localStorage`，统一命名空间前缀 `miansu:`。键：`miansu:jobs`（记录数组）、`miansu:reviews`（复盘，按 jobId）、`miansu:settings`、`miansu:account`、`miansu:theme`、`miansu:seedVer`。
- **逐字稿双路径（重要）**：`detail.html` 提供两条来源，产出**同一份可编辑逐字稿结构** `[{who, text}]` 后共用下游 AI 总结链路：
  - **路径 A · 粘贴逐字稿（默认）**：`parseTranscriptText()` 解析手机录音 App / 讯飞 / 飞书妙记导出的文本，兼容 `面试官：`、`[面试官]`、`00:03 我：` 等格式；未识别说话人的行归入上一位说话人，全无标记则归「我」。质量最高、零等待、零流量。
  - **路径 B · 本地转写**：`ASR.transcribe()` 用 `transformers.js` 在浏览器本地跑 Whisper（模型可在 `small / base / tiny` 间切换，默认 `small`），音频不出浏览器；结果经 `stripHallucination()` 清洗幻觉后归一。
- **AI 接入（可插拔代理层）**：`app.js` 内 `fetch(s.endpoint, …)` 调用一个**代理地址**（由用户在 settings 配置），不在前端直存 Key，规避 CORS 与密钥暴露。代理背后对接 DeepSeek。
- **双模式**：`DEMO` 模式（默认，走本地预设 / mock，零配置即可完整体验全部界面与示例数据）；`LIVE` 模式（配好代理后真实调用 AI）。
- **Prompt 透明墙**：P1 漏斗诊断 / P2 面试总结 / P3 简历改写，提示词在 `app.js` 内集中定义，详情与话术见交接包第 10 节。
- **内容红线（必须遵守）**：全站不出现任何真实个人信息；示例数据公司名已全改为虚构（星澜科技 / 云图智能 / 明澈数字 / 禾风科技 / 澜图互动 / 光年引擎）。**新增示例、文案、占位符一律不得出现真实公司名或真实个人。**

## 3. `app.js` 关键模块（行号仅作定位参考）

- `Store`：localStorage 读写封装（带 `miansu:` 前缀）。
- `seed()`：首次进入写入 6 条示例面试记录（**全虚构公司名**，见 §2 红线）。
- `migrate()`：数据迁移；仅当示例未被改动（6 条且含「星澜科技」）时自动重建，用户自建记录一律保留。
- 代理 / LLM 调用：拼 `s.endpoint` 发 `fetch`，处理 DEMO/LIVE 分支与流式/非流式。
- `uid()` 等工具函数：ID 生成、日期格式化、DOM 辅助（`$`/`$$`）。
- `parseTranscriptText()` / `WHO_ALIAS`：粘贴逐字稿解析器，兼容多种导出格式（见 §2 双路径）。**只认已知说话人别名与方/圆括号标签**，避免「注意：…」这类普通冒号句被误判为说话人。
- `ASR` / `ASR_MODELS` / `readAudio()` / `stripHallucination()`：本地 Whisper 链路与幻觉清洗（小模型在静音段会输出与音频无关的文本）。
- 动效：`IntersectionObserver` 驱动的 `.fade` 入场；`Toast` 轻提示。
- 主题：读取 `miansu:theme` 切 `data-theme`，CSS 变量随主题反色。

## 4. 怎么预览 / 运行

- **最简单**：浏览器直接打开 `index.html`（双击即可）。`DEMO` 模式开箱即用，全部界面、示例数据、动效均可体验，无需任何配置或服务器。
- **AI 实时功能**：在 `settings.html` 填入代理地址并切到 `LIVE` 模式后生效（默认 DEMO 即可看完整产品形态）。
- **字体**：标题用 Google Fonts（Noto Serif SC / Inter / Noto Sans SC），断网时回退本地 `Songti SC` / 系统无衬线，不影响布局。
- **无需 Node / 构建**：`_check.js`、`_pack.py` 仅为作者本地校验 / 打包用，运行网站不依赖它们。

## 5. 给 DeepSeek 的协作建议

1. **复刻 / 重构**：以本包 + 交接包为基准，保持双主题玻璃拟态视觉、8 页面结构、localStorage 数据模型不变。
2. **加功能 / 修 bug**：优先复用 `app.js` 现有 `Store` / 代理层 / Prompt 结构，不要另起一套存储或鉴权。
3. **视觉改动**：只动 `styles.css` 的 `:root` 变量与对应类，勿在 HTML 内联 `<style>` 里另写一套（现有 index.html 已有部分内联 hero 样式，建议逐步收敛回全局）。
4. **红线是硬约束**：任何输出都不得引入真实公司名、真实人名或真实个人简历内容；示例数据维持 6 条虚构公司。

## 6. 接入真 AI（LIVE 已就绪）

`app.js` 的 `LLM.chat()` 已支持 LIVE 模式：配好代理后真实调用 DeepSeek（当前默认模型 `deepseek-v4-flash`）。部署与配置步骤：

1. **部署代理**：`proxy-worker.js` 是 Cloudflare Worker 源码。在 Cloudflare 控制台新建 Worker，粘贴本文件并部署，复制 Worker 地址（形如 `https://miansu-proxy.<子域>.workers.dev`）。
2. **配置 Key（二选一）**：
   - 推荐：在 Worker 的「设置 → 变量」里加 `DEEPSEEK_API_KEY = 你的 DeepSeek Key`，网站设置页 Key 可留空（Key 不进浏览器，最安全）。
   - 或：网站设置页直接填 Key（代理会优先用前端的 Key）。
3. **网站内启用**：`settings.html` → 模式切到 `LIVE` → Endpoint 填 Worker 地址 → 保存。徽章变 `LIVE` 即生效。
4. **验证**：设置页点「测试连接」，弹出「连接成功」即通。

> 注意：`deepseek-chat` 已于 2026-07-24 停用，代码内默认模型已改为 `deepseek-v4-flash`。若自行换模型，确保代理与设置页 `model` 一致。

## 7. 更新日志

版本号定义在 `app.js` 顶部 `APP_BUILD.ver`，设置页底部与 about 页脚会显示它——**这是判断线上是否更新的唯一可靠依据**。

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.5 | 2026-08-31 | 逐字稿双路径（粘贴 / 本地 Whisper 转写）归一到 `[{who, text}]` |
| v1.6 | 2026-09-01 | 说话人匿名标签「说话人N」识别 + AI 判定角色 |
| v1.7 | 2026-09-01 | 说话人角色改为**用户明确指定**（AI 仅预填 + 全同角色护栏） |
| v1.8 | 2026-09-01 | 粘贴后自动 AI 识别角色；逐字稿 / 总结**自动保存**；总结四大点**卡片化**；**聊天框式**逐字稿 |
| v1.8.1 | 2026-09-01 | 解析器支持「说话人标签独占一行」；设置页 + about 页显示构建版本号 |
| **v1.8.2** | **2026-09-01** | **首次通过 Netlify CLI 真正上线**；解析器补三个盲区；about 页 `v2.0` 硬编码改为真实版本号 |

### v1.8.2 解析器补的三个盲区（`parseTranscriptText`）

| 格式 | 示例 | 之前 |
|---|---|---|
| 别名 + 时间戳**无冒号** | `面试官 00:00:28 你好` | ❌ 整行漏判 → 全归「我」 |
| 中文方头 / 直角 / 书名号标签 | `【说话人1】…`、`[张三] …` | ❌ 只认 `[]` `()` |
| 标签行带括号时间戳 | `说话人1 (00:00:28)` 换行接正文 | ❌ |

- 括号标签统一走 `B_OPEN` / `B_CLOSE` 字符类，覆盖 `() [] 【】 「」 『』 ［］ 〈〉 《》 <>`。
- `WHO_ALIAS` 按长度倒序参与正则，避免「面试官」被「面」抢先匹配。
- 防误判保留：「注意：…」不产生说话人；「我自己…」不切出「我」。

### 发版方式（只用这个）

```bash
cd "E:/Workbuddy/2026-08-31-11-09-30"
# 1) 整目录重建部署包（禁止增量 cp，会出半残文件）
FILES="about.html app.js detail.html index.html profile.html proxy-worker.js records.html resume.html review.html settings.html styles.css 源码说明_面溯.md"
rm -rf "面溯-部署用" v2 && mkdir "面溯-部署用" v2
for f in $FILES; do cp "$f" "面溯-部署用/"; cp "$f" v2/; done

# 2) 部署（约 11 秒）
"C:/Users/27782/.workbuddy/binaries/node/versions/22.22.2-2/node.exe" \
  "C:/Users/27782/.workbuddy/binaries/node/workspace/node_modules/netlify-cli/bin/run.js" \
  deploy --prod --dir="面溯-部署用" --site=9095666e-f825-4e97-aef9-e415be265933

# 3) 必须验收：curl 核实线上版本号
curl -s https://miansu.netlify.app/app.js | grep -o "ver: '[^']*'"
```

⚠️ **不要拖文件夹到 Netlify Drop 首页**——会新建匿名站点，原域名仍指向旧部署（v1.8 / v1.8.1 长期未上线的根因）。

### 解析器单元测试

```bash
node _test_parse.mjs                       # 测本地 app.js
curl -s https://miansu.netlify.app/app.js -o _live_app.js
node _test_parse.mjs _live_app.js          # 测线上那份
```

当前 **18 个用例**，覆盖冒号 / 方括号 / 中文方头括号 / 时间戳 / 匿名标签 / 标签独占一行 / 别名+时间戳无冒号 / 防误判等。改 `parseTranscriptText` 后必须重跑。
